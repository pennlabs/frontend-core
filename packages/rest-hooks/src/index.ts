import { useResource, useResourceList } from "./hooks";
import {
  useResourceListResponse,
  useResourceResponse,
  Identifiable,
  Identifier,
} from "./types";

export {
  useResource,
  useResourceList,
  Identifiable,
  Identifier,
  useResourceListResponse,
  useResourceResponse,
};
