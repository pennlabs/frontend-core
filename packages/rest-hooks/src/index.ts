import { useResource, useResourceList } from "./hooks";
import {
  useResourceListResponse,
  useResourceResponse,
  Identifiable,
} from "./types";

export {
  useResource,
  useResourceList,
  Identifiable,
  useResourceListResponse,
  useResourceResponse,
};
